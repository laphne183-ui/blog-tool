# 블로그 순위 체크 프로젝트 노트

## 1. 프로젝트 목적

이 프로그램은 클라이언트의 네이버 블로그 관리 업무 중 반복적으로 발생하는 순위 확인/캡처/보고서 작성 작업을 자동화하기 위해 만들었다.

기존 업무 방식:

- 월간 보고서 작성 시, 메인 키워드를 시크릿 모드 기준으로 네이버 블로그 탭에서 하나씩 검색
- 해당 키워드의 노출 순위를 직접 확인
- 검색 결과 화면을 직접 캡처
- 순위와 이미지를 엑셀 보고서에 수기로 정리

문제점:

- 키워드 수가 많을수록 시간이 오래 걸림
- 사람 손으로 반복 작업을 하다 보니 리소스 낭비가 큼
- 캡처 위치/형식이 일정하지 않을 수 있음

해결 목표:

- 키워드 검색 자동화
- 목표 업체 블로그 카드 자동 매칭
- 검색 결과 화면 자동 캡처
- 순위/캡처 이미지를 보고서 템플릿에 자동 반영

## 2. 기능 확장 배경

프로그램을 운영하다 보니 월간 보고서뿐 아니라 주간 보고 업무도 반복 리소스가 크다는 점이 확인되었다.

기존 주간 보고 방식:

- 주간 발행 키워드를 수기로 정리
- 각 키워드의 노출 여부/순위를 확인
- 카카오톡 보고용 문구를 직접 작성
- 보낼 캡처 이미지를 직접 선별

그래서 주간 보고 기능도 추가했다.

추가 목표:

- 스케줄 엑셀에서 해당 구간 키워드 자동 추출
- 주간 보고용 텍스트 파일 자동 생성
- 30위 이하 키워드만 주간 보고 대상 이미지로 선별

## 3. 현재 주요 기능

- 네이버 블로그 검색 결과에서 목표 업체 카드 매칭
- API 순위 + 화면 순위 수집
- 검색 결과 카드 자동 캡처
- 종료 보고서용 엑셀 파일 자동 생성
- 주간 보고용 텍스트 파일 자동 생성
- 주간 보고용 이미지 별도 폴더 분리 저장

## 4. 현재 동작 방식 요약

### 종료 보고서

1. 보고서 템플릿 파일 선택
2. 업체명/키워드 기준으로 검색 실행
3. 프로그램이 템플릿 기반 결과 파일 생성
4. 캡처본과 순위를 result 시트에 삽입

### 주간 보고

1. 스케줄 엑셀 파일 선택
2. 업체명 입력
3. 주간 범위 선택 (`1~5`, `6~10`, `11~15` ...)
4. 해당 구간 키워드 검색 실행
5. 주간 보고 txt 생성
6. 화면 순위 30위 이하 이미지만 주간 보고 전용 폴더에 복사

## 5. 이번 스레드에서 정리/수정한 내용

### 5-1. 주간 보고 기능 추가 및 보완

- 스케줄 시트에서 `"순"` / `"키워드"` 컬럼을 동적으로 찾아 읽도록 구현
- 주차 선택 방식을 `1주차`, `2주차` 대신 `1~5`, `6~10` 형식으로 변경
- 순번이 20을 넘는 파일도 자동으로 구간 수를 계산해 표시하도록 수정
- 주간 보고 txt 파일 자동 생성
- 미노출 키워드는 txt에서 제외
- 화면 순위 30위 이하만 `1pg (N)` 형식으로 텍스트에 포함

### 5-2. 주간 보고 결과물 저장 구조 변경

기존:

- 원본 캡처 폴더에 txt/이미지가 함께 저장

현재:

- 원본 캡처: `output/captures/업체폴더`
- 주간 보고 전용: `output/주간 보고/업체폴더`

이렇게 분리해서, 카카오톡 발송용 결과물만 따로 바로 확인할 수 있게 변경했다.

### 5-3. 캡처 정책 수정

- 종료 보고서용 캡처는 다시 예전처럼 전체 저장하도록 복구
- 주간 보고는 txt/선별 이미지에서만 30위 이하를 기준으로 제한

즉:

- 종료 보고서 = 원본 캡처 유지
- 주간 보고 = 30위 이하만 별도 선별

### 5-4. 캡처 파일명 순위 보정

캡처 파일명에 화면 순위만 쓰지 않고,

- API 순위와 화면 순위가 둘 다 있으면 더 높은 순위(숫자가 더 작은 값)
- API 순위가 없으면 화면 순위

를 사용하도록 수정했다.

예:

- 화면 5위 / API 4위 -> `_4.png`
- 화면 16위 / API 17위 -> `_16.png`

### 5-5. 템플릿 호환성 이슈 대응

특정 템플릿(예: 최용석행정사사무소 파일)은 `styles.xml` 문제로 openpyxl이 바로 읽지 못하는 문제가 있었다.

대응 내용:

- 정상 템플릿은 일반 방식으로 로드
- 문제가 있는 템플릿만 fallback 방식으로 로드
- fallback 사용 시 저장 후 원본 스타일을 복원하도록 처리
- 이전에 생성되던 `._xlfix.xlsx` 임시 파일은 메모리 처리로 바꿔서 더 이상 남기지 않도록 수정

현재 구조:

- 정상 템플릿 -> 기본 경로
- 문제 템플릿 -> 예외 경로(fallback)

## 6. 현재 기준으로 유지하기로 한 판단

- GUI는 당장 대규모 개편하지 않음
- headless 모드는 유지
  - 팀원들이 기존 브라우저 업무와 병행하므로 새 창이 계속 뜨는 방식은 불편하다고 판단
- exe 용량은 크지만, 내장 브라우저를 사용해 비교적 중립적인 환경에서 검색하는 현재 구조를 유지
- 선택자는 `selectors.json` 외부 설정 파일로 분리해 유지보수 포인트를 낮춤

## 7. 앞으로 기록할 때 기준

앞으로는 모든 과거 이력을 한 번에 복원하기보다, 지금 시점부터 아래만 간단히 남기는 방식으로 기록한다.

- 왜 수정했는지
- 무엇을 바꿨는지
- 결과적으로 어떻게 동작하는지
- 아직 남은 이슈가 있는지

이 문서는 포트폴리오 초안 재료로도 활용할 수 있도록 유지한다.
