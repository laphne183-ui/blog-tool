# CHANGELOG

## 2026-04-19

- 주간 보고 기능 추가
  - 스케줄 시트에서 `"순"` / `"키워드"` 컬럼을 동적으로 읽어 주간 구간 키워드 추출
  - 주간 범위를 `1주차` 대신 `1~5`, `6~10` 형식으로 표시
  - 주간 보고 txt 자동 생성

- 주간 보고 결과물 구조 정리
  - 원본 캡처는 `output/captures/업체폴더` 유지
  - 주간 보고 전용 결과물은 `output/주간 보고/업체폴더`로 분리
  - 주간 보고 txt는 해당 전용 폴더에만 저장
  - 화면 순위 30위 이하 이미지들만 주간 보고 폴더로 복사

- 캡처/순위 처리 수정
  - 종료 보고서용 캡처는 다시 전체 저장하도록 복구
  - 주간 보고 txt는 화면 순위 30위 이하만 `1pg (N)` 형식으로 기록
  - 캡처 파일명은 API 순위와 화면 순위 중 더 높은 순위(숫자가 더 작은 값)를 사용하도록 조정

- 템플릿 호환성 대응
  - 정상 템플릿은 기본 경로로 로드
  - 문제가 있는 템플릿은 fallback 경로로 로드
  - fallback 사용 시 저장 후 원본 스타일 복원
  - `._xlfix.xlsx` 임시 파일이 남지 않도록 메모리 처리로 변경

- 선택자 외부 분리
  - `selectors.json` 파일 추가
  - 블로그 카드/채널/제목 선택자와 무시 링크 라벨을 외부 파일에서 읽도록 변경
  - 네이버 UI 변경 시 exe 재배포 없이 선택자 파일 수정으로 대응 가능하도록 구조 개선

- 문서화 시작
  - `PROJECT_NOTES.md` 추가
  - 프로젝트 목적, 기능 확장 배경, 현재 구조와 판단 내용을 정리
