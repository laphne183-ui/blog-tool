# 작업 일지 (WORKLOG)

## 2026-06-10 — 글단위 매칭 도입 + 소스 유실 복구 + API 범위 확장

### 배경

4월 말부터 클라이언트 블로그 게시물들이 네이버 검색에서 일제히 누락되는 사태 발생.
순위 체크 중 **"화면(통합검색)은 미노출인데 API 순위는 잡히는"** 케이스가 나왔고,
분석 결과 다음 문제가 확인됨:

- API 매칭이 **블로그(출처) 단위**라서, "API 7위"가 화면에서 빠진 바로 그 글이라는
  보장이 없음. 같은 블로그의 **옛 글**이 잡혀도 "노출"로 기록됨.
- 즉 기존 결과로는 "이 글이 색인에 있다"까지 단정할 수 없고,
  "이 블로그가 OpenAPI에 서빙됨"까지만 확실했음.

### 진단 결론 (콤비타 코리아 사례로 검증)

식별값을 글 URL로 바꿔 재검색한 결과:

- 이전에 보이던 API 26위/96위 등은 전부 **같은 블로그의 옛 글**이 잡힌 것이었음 (확정)
- 4월 말 이후 발행 글들은 OpenAPI 300위 안에서도 안 잡힘 → 신규 글이 서빙에서 빠지는 중
- 5/12 글 하나만 API 16위(글단위) / 화면 98위 → 색인은 살아있으나 통합검색 랭킹단에서 억제
- 프로그램 내장 "블로그 누락 확인" 기능(RSS 기반 글단위 체크)의 결과와 일치

**해석**: 수집/색인 실패가 아니라 출처(블로그) 단위 랭킹 억제에 가까움.
색인에 살아있는 글은 삭제·재발행하지 말 것 (억제가 풀리면 회복 가능).

### 코드 변경 (커밋 d5fd963)

1. **글단위 API 매칭** (`naver_api.py`)
   - 식별값이 네이버 글 URL이면 OpenAPI 결과 중 **같은 logNo인 글만** 순위로 인정
   - 결과에 `match_scope` 추가: `post`(글단위) / `blog`(블로그단위)
   - 글 URL인데 못 찾으면 "글단위 미확인" — 블로그단위로 오인하지 않음

2. **글단위 화면 매칭** (`main.py`)
   - 글 URL이 식별값이면 화면 카드도 같은 logNo만 매칭 (같은 블로그의 다른 글 차단)
   - `PostView.naver?logNo=` 쿼리형 링크에서도 글 ID 추출되도록 수정
   - 매칭값/비고에 "API 블로그단위(동일 글 확인 아님)" 등 범위 라벨 표시

3. **API 확인 범위 100→300위** (`main.py`)
   - 키워드당 HTTP 요청 1회→3회로 시간 부담 거의 없음 (느린 건 화면 스크롤 쪽)
   - `config.json`에 `"api_max_results": 500` 식으로 조절 가능 (100~1000)

4. **엑셀 보고서** (`report.py`) — `API매칭범위` 컬럼 추가 (글단위/블로그단위/글단위 미확인)

### 사용 기준

| 식별값 입력 | API 순위의 의미 |
|---|---|
| 업체명/블로그명 | 그 **블로그**가 OpenAPI에 서빙됨 (옛 글일 수 있음) |
| **글 URL** (`blog.naver.com/아이디/글번호`) | 바로 **그 글**이 색인에 있고 N위로 서빙됨 |

- 누락 진단 시에는 반드시 **글 URL**을 식별값에 넣을 것
- "글단위 미확인" = OpenAPI 300위 안에 없음 (색인 완전 부재 단정은 아님)
- 클라이언트 보고는 **화면순위(실노출)** 기준으로만 할 것. API 순위는 내부 진단용

### 소스 유실 사고 복구 기록

- 배포 exe(5/28)는 구버전 로컬 소스 + **exe 바이트코드 직접 패치**로 운영되어 와서,
  "블로그 누락 확인"·"주간 보고" 기능의 소스가 로컬 디스크에 없었음
- 6/10 코덱스가 구버전 소스로 재빌드하면서 위 기능들이 빌드에서 소실됨
- **이 깃허브 레포가 유일한 최종 소스**로 확인되어, 여기에 신규 수정을 이식 후 재빌드로 해결
- 교훈: **모든 수정은 이 레포에 커밋 → 레포 소스로만 빌드.** exe 직접 패치 금지

### 빌드·배포 절차 (Windows PC에서)

```
git pull
python -m PyInstaller --noconfirm --clean blog_tool.spec
# 산출물: dist\blogranker\블로그순위체커.exe + _internal\
```

배포 폴더 구성 (exe 옆에 있어야 하는 것들):

```
블로그 순위 체크.exe   ← 블로그순위체커.exe 이름 변경
_internal\            ← 빌드 산출물
browsers\             ← Playwright Chromium (리비전 1208, 기존 배포본에서 복사)
config.json
selectors.json
input.csv
사용 방법.txt / 사용안내.html
```

- 현재 배포 위치: `D:\blog_analyzer\blog_tool\_dist_blogranker_new\blogranker`
- 기존 운영본(드롭박스 `※ 블로그 순위 체크 프로그램`)은 검증 완료 전까지 보존
