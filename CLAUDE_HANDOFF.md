# 블로그 순위 체커 인수인계 메모

## 1. 프로그램 개요

이 프로젝트는 네이버 블로그 검색 노출을 확인하는 Windows용 파이썬 GUI 프로그램입니다.

핵심 동작은 아래 순서입니다.

1. 입력된 업체명 / 키워드 / 식별값 목록을 읽음
2. 네이버 블로그 API로 대상 블로그의 검색 순위를 확인함
3. Playwright 브라우저로 실제 네이버 검색 화면에 들어가 블로그 탭 기준 노출 순위를 찾음
4. 찾은 카드 화면을 캡처함
5. 결과를 엑셀 파일로 저장함
6. 보고서 템플릿이 있으면 캡처 이미지와 순위를 끼워 넣어 최종 보고서를 생성함

## 2. 현재 확인한 상태

- GUI 앱이 이미 구현되어 있음
- 실행 엔진도 구현되어 있음
- `dist/blogranker/` 폴더에 빌드 결과물이 존재함
- `output/` 폴더에 실제 실행 결과물과 캡처 이미지가 존재함
- 템플릿 기반 엑셀 보고서 자동 생성 기능도 구현되어 있음

즉, “초기 골격” 단계는 아니고 실제 업무에 맞춰 꽤 많이 완성된 상태입니다.

## 3. 주요 파일 역할

### 실행 / UI

- `app.py`
  - `customtkinter` 기반 메인 GUI
  - API 키 입력/저장
  - 입력 행 관리
  - 검색 시작/중지/일시정지
  - 로그 표시
  - 보고서 템플릿 선택 및 최종 보고서 생성

### 검색 엔진

- `main.py`
  - 실제 검색 실행 로직
  - 네이버 API 조회
  - 브라우저 자동화
  - 화면 스크롤 탐색
  - 캡처 저장
  - 기본 결과 엑셀 저장

- `browser.py`
  - Playwright 브라우저 래퍼
  - 검색 페이지 이동, 블로그 탭 클릭, 스크롤, 스크린샷 처리

- `naver_api.py`
  - 네이버 블로그 검색 API 호출
  - 식별값 기준 매칭

### 엑셀 출력

- `report.py`
  - 기본 결과 엑셀 저장
  - 전체 결과 / 오류 / 미노출 시트 생성

- `excel_writer.py`
  - 보고서 템플릿의 `schedule`, `result` 시트를 분석
  - 캡처 이미지 삽입
  - 키워드별 순위 입력
  - 최종 보고서 저장

### 빌드

- `blog_tool.spec`
  - PyInstaller 설정

- `build.bat`
  - exe 빌드 및 Playwright 브라우저 복사

## 4. 실행 방식

### 개발 환경 실행

메인 GUI는 아래 파일입니다.

- `app.py`

실행 시 `config.json`, `input.csv`, `output/`를 기준으로 동작합니다.

### 배포본

현재 배포본 위치:

- `dist/blogranker/블로그순위체커.exe`

배포 시 브라우저까지 포함된 폴더 단위로 전달하는 구조입니다.

## 5. 입출력 파일 구조

### 입력

- `config.json`
  - 네이버 API 키
  - 브라우저 모드
  - 저장된 행 목록

- `input.csv`
  - 검색 대상 목록

### 출력

- `output/rank_report.xlsx`
  - 기본 결과 엑셀

- `output/captures/...`
  - 키워드별 캡처 이미지

- `output/*완성_YYYYMMDD_HHMMSS.xlsx`
  - 템플릿 기반 최종 보고서

## 6. 확인한 구현 포인트

- 브라우저 모드 `auto / chrome / chromium` 지원
- API 결과와 실제 화면 결과를 둘 다 저장
- 미노출 / 오류 결과 분리 저장
- 캡처 파일명을 키워드 기준으로 정리
- 템플릿의 셀 위치를 하드코딩하지 않고 어느 정도 자동 분석함
- GUI에서 API 키를 입력하면 런타임 환경변수로 적용되도록 연결돼 있음

## 7. 주의할 점

### 1) `config.json`에 실제 API 키가 들어 있음

현재 `config.json`에는 네이버 API `client_id`, `client_secret` 값이 들어 있습니다.

그래서 이 프로젝트를 다른 사람에게 그대로 전달하거나 업로드할 때는:

- API 키를 비우고 전달하거나
- 별도 개인 설정 파일로 분리하는 게 안전합니다

### 2) 의존성 목록 파일이 없음

현재 기준으로는 `requirements.txt`가 보이지 않았습니다.

코드상 필요한 주요 패키지는 대략 아래입니다.

- `pandas`
- `requests`
- `playwright`
- `openpyxl`
- `Pillow`
- `customtkinter`

새 PC에서 개발 환경을 다시 세팅할 때는 의존성 정리가 한 번 필요합니다.

### 3) Git 저장소는 아직 아님

현재 폴더는 `.git`이 없어 버전 관리가 안 붙은 상태입니다.

Claude에서 작업하던 히스토리를 이어가거나 백업하려면 Git 초기화가 있으면 훨씬 관리가 편해집니다.

## 8. 지금 단계에서의 판단

이 프로그램은 “아이디어만 있는 상태”가 아니라 아래까지 도달해 있습니다.

- 실제 GUI 있음
- 실제 검색 로직 있음
- 실제 캡처 저장됨
- 실제 엑셀 결과물 생성됨
- exe 빌드본 존재함

즉, 바로 다음 작업은 새로 만들기보다:

1. 문서화
2. 의존성 정리
3. API 키 분리
4. 예외 처리 다듬기
5. 테스트용 샘플 데이터 정리

이런 마무리/안정화 쪽이 더 맞습니다.

## 9. 다음 추천 작업

우선순위 기준 추천:

1. `README.md` 작성
2. `requirements.txt` 생성
3. `config.json`에서 실제 비밀키 제거
4. 샘플 `config.example.json` 추가
5. 실행 실패 케이스 점검
6. Git 저장소 초기화

## 10. 이번 점검에서 직접 확인한 산출물

- 빌드 폴더 존재: `dist/blogranker/`
- 실행 파일 존재: `dist/blogranker/블로그순위체커.exe`
- 캡처 이미지 다수 존재: `output/captures/...`
- 최종 보고서 엑셀 다수 존재: `output/*완성*.xlsx`

## 11. 한 줄 요약

Claude에서 만들던 작업은 상당 부분 완성돼 있고, 지금은 “재개발”보다 “정리해서 이어받기 좋은 상태로 만드는 단계”에 가깝습니다.
